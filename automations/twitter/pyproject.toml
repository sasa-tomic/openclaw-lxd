[project]
name = "twitter"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "langdetect>=1.0.9",
    "prefect>=3.6.18",
    "psycopg2-binary>=2.9.11",
    "requests>=2.32.5",
    "websocket-client>=1.8.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

[tool.pytest.ini_options]
markers = [
    "integration: slow tests that use real CDP, LLM, and DB (deselect with -m 'not integration')",
]
